# FileConverter

The FileConverter is a small program that crawls a directory and its subdirectories, converting all found video files to h264, to make them streamable using a Jellyfin-Server.

## Execute
* Edit the Filepath in the code to your servers video directory.
* run code in an ide or via the terminal

## Future Features
* Implementation using C++ for improving speed
* Adding a UI for better UX
